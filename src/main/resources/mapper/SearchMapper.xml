<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.keepfight.Mapper.SearchMapper">

    <insert id="insert" parameterType="SearchDao">
        INSERT INTO search(sear, time)
        VALUES ( #{sear}, #{time})
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            SELECT last_insert_rowid();
        </selectKey>
    </insert>

    <select id="prompt" resultType="String" parameterType="String">
        SELECT
            sear,
            count(sear)
        FROM search
        WHERE sear LIKE #{xx}
        GROUP BY sear
        ORDER BY count(sear) DESC
        LIMIT 5
    </select>
</mapper>