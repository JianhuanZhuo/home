<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.keepfight.Mapper.ArticleMapper">

    <insert id="insert" parameterType="ArticleDao">
        INSERT OR IGNORE INTO article(url, crawltime, updatetime, pubtime, title, pic, abs, file, cmt_id,
        cmt_num,
        hash,
        source)
        VALUES (#{url}, #{crawltime}, #{updatetime}, #{pubtime}, #{title}, #{pic}, #{abs}, #{file}, #{cmt_id},
        #{cmt_num},
        #{hash},
        #{source})
        <selectKey keyProperty="id" order="AFTER" resultType="int">
            SELECT last_insert_rowid();
        </selectKey>
    </insert>

    <select id="pickArticle" resultType="ArticleDao">
        SELECT *
        FROM article
        WHERE file ISNULL
            AND source='QQ'
        ORDER BY updatetime
            ASC
        LIMIT 1
    </select>

    <select id="pickArticleForIndex" resultType="ArticleDao" parameterType="int">
        SELECT *
        FROM article
        WHERE file NOTNULL
              AND id > #{last_id}
              AND source = 'QQ'
        ORDER BY id
            ASC
        LIMIT 100
    </select>

    <select id="mostHotArticles" resultType="ArticleDao">
        SELECT *
        FROM article
        ORDER BY pubtime
            DESC
        LIMIT 100
    </select>

    <select id="pickArticleForComment" resultType="ArticleDao">
        SELECT *
        FROM article
        WHERE cmt_id ISNULL
        ORDER BY updatetime
            ASC
        LIMIT 1
    </select>

    <select id="listUrl" resultType="String">
        SELECT url
        FROM main.article
    </select>

    <update id="stickByID" parameterType="ArticleDao">
        UPDATE article
        SET
            pubtime   = #{pubtime},
            source    = #{source},
            crawltime = #{crawltime},
            title     = #{title},
            pic       = #{pic},
            abs       = #{abs},
            cmt_id    = #{cmt_id},
            cmt_num   = #{cmt_num},
            hash      = #{hash},
            file      = #{file}
        WHERE id = #{id}
    </update>

    <update id="updateLastByID">
        UPDATE article
        SET
            updatetime = #{updatetime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM article
        WHERE id = #{id}
    </delete>
</mapper>